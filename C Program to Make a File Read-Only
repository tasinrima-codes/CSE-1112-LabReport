#include <stdio.h>
#include <sys/stat.h>

int main() {
    if (chmod("file.txt", 0444) == 0)
        printf("File is now read-only\n");
    else
        printf("Failed to change file permission\n");
    return 0;
}
