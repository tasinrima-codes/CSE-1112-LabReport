#include <stdio.h>

int main() {
    int n, temp, r, rev = 0;
    scanf("%d", &n);

    temp = n;

    while (temp != 0) {
        r = temp % 10;
        rev = rev * 10 + r;
        temp /= 10;
    }

    if (rev == n)
        printf("Palindrome\n");
    else
        printf("Not Palindrome\n");

    return 0;
}
