#include <stdio.h>

int main() {
    int h, m;
    printf("Enter time (HH MM): ");
    scanf("%d %d", &h, &m);
    if (h >= 12)
        printf("%02d:%02d PM\n", (h > 12) ? h - 12 : h, m);
    else
        printf("%02d:%02d AM\n", (h == 0) ? 12 : h, m);
    return 0;
}
