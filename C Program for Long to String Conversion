#include <stdio.h>

int main() {
    long num;
    char str[50];
    printf("Enter a long number: ");
    scanf("%ld", &num);
    sprintf(str, "%ld", num);
    printf("Long as string: %s", str);
    return 0;
}
