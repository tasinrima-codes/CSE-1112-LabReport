#include <stdio.h>
int main() {
    char name[50];
    printf("Enter your full name: ");
    gets(name);
    printf("Initials: %c", name[0]);
    for (int i = 0; name[i] != '\0'; i++)
        if (name[i] == ' ')
            printf("%c", name[i + 1]);
    return 0;
}
