#include <stdio.h>

int main() {
    int a, b, i, j;

    scanf("%d %d", &a, &b);

    for (i = a; i <= b; i++) {
        int flag = 0;

        if (i <= 1) continue;

        for (j = 2; j < i; j++) {
            if (i % j == 0) {
                flag = 1;
                break;
            }
        }

        if (flag == 0)
            printf("%d ", i);
    }

    return 0;
}
